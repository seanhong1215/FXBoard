# Frankfurter v1 API（免費、免 API key）
FX_API_BASE=https://api.frankfurter.dev/v1

# 外部 API timeout（毫秒）
FX_API_TIMEOUT_MS=8000

# 單一 Serverless instance 的記憶體快取 TTL（毫秒）
FX_RATES_TTL_MS=60000
FX_HISTORY_TTL_MS=3600000

# 基本單實例 IP 限流；免費方案不依賴外部 Redis/KV
FX_RATE_LIMIT_MAX=60
FX_RATE_LIMIT_WINDOW_MS=60000
